spring:
  application:
    name: pomki
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:pomki;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
  data:
    redis:
      host: localhost
      port: 6379
      password: 
      username: 

server:
  port: 8080

logging:
  level:
    com.cooltomato.pomki: DEBUG
    org.springframework.data.redis: DEBUG

# JWT 설정
jwt:
  secret-key: pomki-secret-key-for-development-only-do-not-use-in-production
  access-token-expiration: 3600000  # 1시간
  refresh-token-expiration: 604800000  # 1주일

# OAuth2 설정 (개발용 - 실제 값은 별도 환경변수로 관리)
oauth2:
  google:
    client-id: your-google-client-id
    client-secret: your-google-client-secret
  kakao:
    client-id: your-kakao-client-id
    client-secret: your-kakao-client-secret

# AI 설정 (OpenAI API)
ai:
  openai:
    api-key: # OpenAI API 키를 여기에 입력하세요
    model: gpt-3.5-turbo 